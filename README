# ============================================================
# task_queue.txt 使用说明
#
# 【用途】
# 这是任务入口文件（唯一需要人工维护的文件）
# 新任务写在最上面，编号从大到小排列
#
# 【基本规则】
# 1. 新任务永远写在文件最上面（表头下面一行）
# 2. NUMBER 必须递增（新的编号更大）
# 3. STATE 初始必须写为 PENDING
# 4. dispatcher 会自动把：
#       PENDING -> RUNNING -> DONE
# 5. 不要手动修改 RUNNING / DONE 状态
# 6. TEST_TARGET 路径不要包含空格
# 7. SERVER 必须是已配置服务器（如 pudong / kangqiao）
#
# 【字段说明】
#
# NUMBER
#   任务编号（整数，递增）
#
# SERVER
#   目标服务器名称（必须匹配 dispatcher 配置）
#
# TEST_TYPE
#   输入类型（4种）：
#     all           -> 跑整个 test2 目录
#     case_dir      -> 输入某个 case 目录
#     run_tcl       -> 输入具体 run.tcl 文件
#     abs_dir_list  -> 输入一个文件，里面是路径列表
#
# TEST_TARGET
#   根据 TEST_TYPE 不同：
#     all           -> 必须写 "."
#     case_dir      -> /path/to/case_dir
#     run_tcl       -> /path/to/run.tcl
#     abs_dir_list  -> /path/to/list.txt
#
# STAGES
#   逗号分隔的阶段列表，例如：
#     place_design,route_design
#
#   支持：
#     opt_design
#     place_design
#     route_design
#     phys_opt_design
#     write_checkpoint
#     write_bitstream
#     report_timing_summary
#     bit_cmp
#     msk_cmp
#     bgn_cmp
#
#   特殊规则：
#     如果包含 write_bitstream
#     会自动启用：
#       bit_cmp, msk_cmp, bgn_cmp
#
# BG
#   并行线程数，例如：
#     8 / 12 / 16
#
# STATE
#   状态（只允许三种）：
#     PENDING   -> 等待执行（人工写入）
#     RUNNING   -> 正在执行（自动修改）
#     DONE      -> 已完成（自动修改）
#
# ============================================================

NUMBER SERVER TEST_TYPE TEST_TARGET STAGES BG STATE

# 示例（新任务写在这里 ↓↓↓）
12 pudong all . place_design,route_design 12 PENDING
11 kangqiao run_tcl /home/chenggong/workspace/cases/aes_top/run.tcl route_design,write_bitstream 12 PENDING
10 pudong abs_dir_list /home/chenggong/workspace/lists/fail.txt phys_opt_design,route_design 12 DONE