#!/bin/bash

BG_MAX=12
TIME_LIMIT=10800
# Stop a runaway case whose directly-written run log reaches this size.
# The runner terminates the case process group instead of piping through head,
# because checksum_cmp reads run while GalaxCore is still running.
# Set GALAXCORE_MAX_CASE_LOG_MB=0 only when an uncapped run log is required.
MAX_CASE_LOG_MB="${GALAXCORE_MAX_CASE_LOG_MB:-2048}"
export MAX_CASE_LOG_MB
GALAXCORE_BIN="$WORKSPACE_ROOT/../bin/Linux_64/GalaxCore"
REPORT_DST_DIR="$RUNTIME_DIR/exports"
FLOW_CONFIG_ABS=""
