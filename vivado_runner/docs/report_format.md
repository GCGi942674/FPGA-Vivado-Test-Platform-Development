# Report layout

Latest reports are written to `runtime/reports/latest/`:
- `runTime_Summary`
- `list_fail_to_run`
- `stat_summary`
- `execution_report.txt`
- `execution_report.json`

Each run is also archived under `runtime/reports/archive/<timestamp>/`.
